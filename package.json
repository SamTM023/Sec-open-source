{
  "name": "sec-open-source",
  "version": "0.1.0",
  "private": true,
  "description": "Security Scanner Hub - frontend voor open source security tooling (SAST/DAST)",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "build": "npm --workspace shared run build && npm --workspace backend run build && npm --workspace frontend run build",
    "lint": "npm --workspace frontend run lint && npm --workspace backend run lint"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "fast-xml-parser": "^5.7.2"
  }
}
